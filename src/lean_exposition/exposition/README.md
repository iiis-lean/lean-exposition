# exposition

Planned responsibility: ScopeView and WriteContext assembly, sequential section/entry generation, anchors and rendering.

Scaffold only. No behavior or API is implemented. See the local consolidated design in `dev_docs/design/`.
