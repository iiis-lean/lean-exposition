# features

Production deterministic declaration observations, explicit native compiled probes,
and deduplicated node summaries. Feature artifacts bind to workspace and hierarchy
digests plus `FEATURE_CONFIG_DIGEST`; there is no extractor version compatibility
layer. Public APIs: `FeatureSet`, `extract_features`, `collect_native_features`.
See [feature documentation](../../../docs/features.md).
