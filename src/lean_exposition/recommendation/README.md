# recommendation

Generation-free structural ranking and deterministic random comparison.
Public APIs: `RecommendationConfig`, `make_structural_policy`, `random_policy`.
The current policy IDs are `structural` and `random`; structural settings carry a
canonical configuration digest.
See [recommendation documentation](../../../docs/recommendation.md).
