# interfaces

Planned responsibility: Shared application operations exposed through future CLI, MCP and web-facing API adapters.

Scaffold only. No behavior or API is implemented. See the local consolidated design in `dev_docs/design/`.
